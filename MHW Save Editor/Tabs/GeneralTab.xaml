<UserControl x:Class="MHW_Save_Editor.GeneralTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MHW_Save_Editor"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ListBox Name="ListBox" >
            <Label Name="ChecksumLabel" Content="{Binding Checksum}"/>
            <Label Name="GeneratedChecksumLabel" Content="{Binding OnFileChecksum}"/>
            <Label Name="SteamIdLabel" Content="{Binding SteamId}" MouseDoubleClick="EditSteamLabel"/>
            <Label Name="SizeLabel" Content="{Binding Size}"/>
            <Label Name="FilePathLabel" Content="{Binding FilePath}"/>
        </ListBox>
    </Grid>
</UserControl>
